<Page
    x:Class="ExampleProject.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ExampleProject"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
		<Grid.RowDefinitions>
			<RowDefinition Height="*" />
			<RowDefinition Height="*" />
		</Grid.RowDefinitions>
		<StackPanel Margin="24">
			<StackPanel Orientation="Horizontal" Margin="0, 0, 0, 12">
				<Button Click="SendMessage">SendMessage</Button>
				<Button Click="CallHandler">CallHandler</Button>
			</StackPanel>
			<TextBlock Text="Output:" FontSize="24" TextWrapping="WrapWholeWords" />
			<ScrollViewer Height="400">
				<TextBlock Name="OutputTextBlock" FontSize="{StaticResource ControlContentThemeFontSize}" TextWrapping="WrapWholeWords" />
			</ScrollViewer>
		</StackPanel>
		<WebView Grid.Row="1" Name="TestWebView" />
    </Grid>
</Page>